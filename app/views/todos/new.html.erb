<!--
Coco Carragher, Gi Cort Grasso, Karla Ganon
  This page allows users to create a new ToDo item with the following features:
  - Displays a form for creating a new todo item, which includes input fields for title, category, and priority.
  - Renders validation error messages in case of any issues when saving the new todo item.
  - Provides a "Back" link that navigates users back to the list of all todos.
  The form is rendered using a shared partial (_form.html.erb) for code reusability.
-->

<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h2 class="mb-4">New To-Do Item</h2>
      
      <%= render 'form', todo: @todo %>
      
      <%= link_to 'Back', todos_path, class: "btn btn-outline-blclr mt-3" %>

      <% if @todo.errors.any? %>
        <div class="alert alert-danger">
          <h4><%= pluralize(@todo.errors.count, "error") %> prevented saving:</h4>
          <ul>
            <% @todo.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

    </div>
  </div>
</div>