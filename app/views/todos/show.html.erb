<!--
Coco Carragher, Gi Cort Grasso, Karla Ganon
  This page displays  information about a single To-Do item, including:
  - The title of the ToDo.
  - Information about the ToDos category, priority, and completion status.
  - Option buttons to Edit or Delete the To-Do item.
  The page provides clear indicators for each piece of information, using a card layout for better presentation.
-->

<div class="container">
  <h1 class="my-4" style="text-decoration: underline;"><%= @todo.title %></h1>
  <div class="card card-body mb-4">
    <p>
      <strong>Category:</strong> <%= @todo.category&.name || "Uncategorized" %>
    </p>
    <p>
      <strong>Priority:</strong> <%= @todo.priority ? "Yes" : "No" %>
    </p>
    <p>
      <strong>Status:</strong> <%= @todo.completed ? "Completed" : "Pending" %>
    </p>
  </div>

  <div class="d-flex gap-2 mb-4">
    <%= link_to "Edit", edit_todo_path(@todo), class: "btn btn-blclr" %>
    <%= button_to "Delete", @todo, method: :delete, class: "btn btn-redclr" %>
  </div>
</div>