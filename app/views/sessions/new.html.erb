<!-- 
Coco Carragher, Gi Cort Grasso, Karla Ganon
Main container with top margin using Bootstrap followed by a lot of formatting -->
<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <h2 class="mb-4">Login</h2>
      <%= form_with url: login_path do |form| %>
        <div class="mb-3">
          <%= form.label :username, class: "form-label" %>
          <%= form.text_field :username, class: "form-control", required: true %>
        </div>
        <div class="mb-3">
          <%= form.label :password, class: "form-label" %>
          <%= form.password_field :password, class: "form-control", required: true %>
        </div>
        <%= form.submit "Login", class: "btn btn-grclr" %>
      <% end %>
    </div>
  </div>
</div>